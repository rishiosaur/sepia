
# Some basic type examples.
# We'll be going through casting, and everything else.

# Booleans are exactly how you might expect them to be in other languages.
value x = true

# Let's try converting it to a string!
value x = string(x)

# Because we've converted it to a string, we can log it using `print`.
print("The value of x is: " + x) 